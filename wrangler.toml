name = "fluencerfinder"
compatibility_date = "2024-09-23"

# Assets-only Worker: serves the built SPA. The frontend calls the Render API
# directly via the VITE_API_URL baked in at build time, so no proxy is needed.
[assets]
directory = "creator-discovery/frontend/dist"
not_found_handling = "single-page-application"
