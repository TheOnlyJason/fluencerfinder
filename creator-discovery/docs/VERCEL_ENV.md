# Add these in Vercel → Project → Settings → Environment Variables
# (Production + Preview). Do NOT commit real values to git.

SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_DB_PASSWORD=
SUPABASE_POOLER_HOST=
SUPABASE_POOLER_PORT=5432
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4o-mini
TAVILY_API_KEY=
YOUTUBE_API_KEY=
APP_ENV=production
CORS_ORIGINS=https://fluencerfinder.vercel.app,http://localhost:5175

# VITE_API_URL is NOT needed when API runs on the same Vercel domain.
