# Backend API URL for production (Vercel). Leave empty for local dev — Vite proxy handles /search, /accounts, etc.
# Example after deploying backend to Render:
# VITE_API_URL=https://fluencerfinder-api.onrender.com
